{
  "name": "@validation-poc/build",
  "version": "0.0.0",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "build": "npm run clean && tsc --build tsconfig.json",
    "clean": "rimraf dist/*"
  },
  "dependencies": {
  },
  "devDependencies": {
    "rimraf": "~3.0.2"
  }
}
